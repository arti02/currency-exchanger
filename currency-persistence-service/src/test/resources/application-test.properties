spring.application.name=currency-persistence-service-test

# keep it simple in tests
app.query-name=currency.queue

# Disable auditing in tests (Java 24 strong encapsulation can break reflective constructor access)
spring.data.mongodb.auditing.enabled=false

# optional: make test logs quieter
logging.level.org.springframework=INFO
